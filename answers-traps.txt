1. a0 - a7  a2
2. 从26:	45b1                	li	a1,12看出，f, g在调用的时候被Inline优化了，直接向a1载入了12
3. 0000000000000630 <printf>:可知printf在630位置上，auipc将其当前pc中的值(0x30)加上0左移12位后赋给了ra，ra经过偏移便指向了printf
4. jalr将下一条指令储存在ra中，一条指令的大小为4，因此可以直接用0x34 + 4 = 0x38(34是根据30推出的)
5. HE110 World
6. printf的format存储在a0，第一个参数3存储在a1，当打印第二个参数时，就会把a2中的trash打印出来